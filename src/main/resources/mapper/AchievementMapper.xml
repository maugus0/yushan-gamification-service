<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yushan.gamification_service.dao.AchievementMapper">

    <select id="findById" resultType="com.yushan.gamification_service.entity.Achievement">
        SELECT
            id,
            name,
            description,
            criteria_json AS criteriaJson,
            icon_url AS iconUrl
        FROM
            achievements
        WHERE
            id = #{id}
        LIMIT 1
    </select>

    <select id="findAll" resultType="com.yushan.gamification_service.entity.Achievement">
        SELECT
            id,
            name,
            description,
            criteria_json AS criteriaJson,
            icon_url AS iconUrl
        FROM
            achievements
    </select>

    <insert id="insert" parameterType="com.yushan.gamification_service.entity.Achievement">
        INSERT INTO achievements (id, name, description, criteria_json, icon_url)
        VALUES (#{id}, #{name}, #{description}, #{criteriaJson,jdbcType=OTHER,typeHandler=org.apache.ibatis.type.StringTypeHandler}, #{iconUrl})
    </insert>

</mapper>